# Supabase — S.P.A.M project (platform home). App uses the `produccion` schema.
NEXT_PUBLIC_SUPABASE_URL=https://ybbrpqzbedaxsmotgtkh.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_DB_SCHEMA=produccion

# Notifications — email vía Gmail SMTP (patrón "Brooklyn" de SnapTrack).
# Cuenta REAL de Google Workspace + App Password de 16 chars → Gmail resuelve
# SPF/DKIM/DMARC (cero verificación de dominio). El repo es PÚBLICO: estos valores
# van SÓLO en env (Vercel / .env.local), jamás commiteados.
GMAIL_USER=unique@runna.com.mx
GMAIL_APP_PASSWORD=
APP_URL=https://runna-greenlight.vercel.app
# (Slack: pendiente de una fase futura)
SLACK_BOT_TOKEN=

# Gary AI check (P4)
ANTHROPIC_API_KEY=

# Google auth domain allowlist (comma-separated)
AUTH_ALLOWED_DOMAINS=rvsnappad.com,runnareach.com
