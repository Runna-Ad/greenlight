[2026-08-23] DISCOVERY (verificar un deploy de Vercel: UN check directo, NO un loop rápido de `vercel ls`): un poll en background que corre `npx vercel ls` en loop (cada ~18-20s, 10-12 iteraciones) para esperar "● Ready" FALSEA-NEGATIVO de forma consistente (pasó 3× en una sesión) — probablemente rate-limit/lentitud/banner "new version available" del CLI hace que la línea salga vacía en la mayoría de las iteraciones, así que nunca matchea y sale por TIMEOUT (exit 2) aunque el deploy SÍ quedó Ready. El método CONFIABLE: **un solo `npx vercel ls 2>&1 | grep vercel.app | head -1`** (la fila más nueva trae "● Ready"/"● Building"/"● Error") + un `curl -s -o /dev/null -w "%{http_code}"` al dominio de prod (runna-greenlight.vercel.app/login → 200 = sirviendo; Vercel sólo re-apunta el alias de prod tras un build EXITOSO, así que alias→build-nuevo + 200 = live). Dar ~40-60s tras el push antes del check (el build tarda ~35s). → USE WHEN: confirmar que un `git push main` llegó a Ready en Vercel. NO montar un loop de polling de `vercel ls`. → SOURCE: ships de H.Ü.E Fase 1/2 + discard (3 falsos timeouts). Emparenta con [[stop-the-old-background-task-when-you-relaunch-it]].

[2026-08-21] DISCOVERY (Notion legales — estructura del doc + cómo verificar el parser sin server ni prod): el doc de legales de DiDi (Notion PAGE, no database; id `201724f6-221e-8045-ace4-db7b6e590863`) tiene 2 headings de marca (heading_2 color naranja: "DiDi Préstamos"/"DiDi Card"); cada legal = un `paragraph` con annotation UNDERLINE (el label = llave + título) y su cuerpo llega en DOS formas: **Préstamos anida el cuerpo como HIJO del label**; **Card lo pone como `bulleted_list_item` HERMANO** que sigue al label. Un `callout` bajo la marca (el 👀) = nota/regla, no legal. El texto se saca VERBATIM de `rich_text[].plain_text` (preserva `*`, "Consulta"/"*Aplican"/"Aplican" — fact-shaped). API: `GET /v1/blocks/{id}/children` (paginar `has_more`/`next_cursor`, recursar `has_children`), headers `Authorization: Bearer` + `Notion-Version: 2022-06-28`. → CÓMO VERIFICAR el parser REAL contra el doc en vivo sin arrancar Next (server-only bloquea node) ni tocar prod: separá el parser PURO (sin `import "server-only"`) en su propio archivo, y en un script de scratchpad transpilalo con `typescript` (`ts.transpileModule`, resuelto vía `createRequire("<proyecto>/package.json")` porque el script vive fuera del repo) → `import()` del .mjs resultante → corré `parseLegales(treeReal)`. Token vía `node --env-file=.env.local` (nunca se imprime). → USE WHEN: parsear cualquier doc de Notion, o verificar lógica pura de parseo contra datos vivos. → SOURCE: Notion→Legales Fase A.

[2026-08-21] DISCOVERY: el MCP de Supabase (`execute_sql`) corre en transacción READ-ONLY → NO puede INSERT/UPDATE/DELETE ("cannot execute … in a read-only transaction"). Para ESCRIBIR/borrar datos en prod (cleanup, backfill — no migración de esquema) usá el CLI con login persistente: escribí el SQL a un archivo y `supabase db query --linked --file <tmp> -o json` (mismo path que reset-blank-slate.mjs y migrate.mjs). Un `with del as (delete … returning …) select … from del group by rollup(...)` reporta cuántas filas borró por categoría. VERIFICÁ el proyecto linkeado ANTES (`cat supabase/.temp/project-ref` → debe ser `ybbrpqzbedaxsmotgtkh`; `supabase projects list` marca ● el LINKED) — es la trampa clásica de borrar en el proyecto equivocado. Los SELECT de verificación (antes/después) sí van bien por el MCP (read-only). → USE WHEN: cualquier data-op de prod que no sea migración de esquema. → SOURCE: biblioteca "sólo Legales" (borrado de 34 snippets no-legal).

[2026-08-19] DISCOVERY: verificar el RENDER de un componente React SIN prod ni dev-server (preview flaky + regla #8 "no probar en prod"): transpilar el .tsx REAL con `typescript` (ya está en node_modules — `ts.transpileModule` con `jsx: ReactJSX`), reescribir el import del alias `@/` al archivo transpilado local, y renderizar con `react-dom/server.renderToStaticMarkup`. Prueba el OUTPUT real (`<strong>`, composición, keys) determinista, sin DB ni servidor. El driver .mjs debe correr con cwd=raíz-del-proyecto (o vivir dentro) para que los bare-imports resuelvan desde node_modules del proyecto. → USE WHEN: hay que probar que un componente pinta lo esperado y el preview no coopera o tocaría datos de prod. → SOURCE: bold-detection (guion pegado), scratchpad/render-check*.mjs.

[2026-07-30] DISCOVERY: PGlite (@electric-sql/pglite) = real Postgres in WASM, runs in Node, no Docker. → USE WHEN: local DB testing when Docker/native Postgres unavailable (this machine has neither). Supports schemas, plpgsql, triggers, roles. Test migrations + naming function + RPCs + filename contract test against it. Caveat: no Supabase auth.* schema — stub auth.users + auth.uid() in the harness. Full RLS-enforcement + Supabase extensions still verify on real DB later. → SOURCE: P1 setup.
